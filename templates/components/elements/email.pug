if field.write
  form
    input(class="inline-block" type="email" value=value name=handle class="pure-g-u-1-2")
    button(class="quickview-field-save__btn" 
      hx-post="/actions/content/update"
      hx-vars="id:"+entity.id
      hx-params="id, "+handle
      hx-swap="none") 🖫
else
  a(href="mailto:"+value)
